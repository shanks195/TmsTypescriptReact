import { makeStyles } from "@mui/styles";

const FormOperatableTreeStyle = makeStyles(() => ({
  root: {
    position: "relative",
    height: '100%',
  },
}));
export default FormOperatableTreeStyle;
